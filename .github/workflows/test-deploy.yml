name: Test Product deployment
run-name: Test Product deployment 

on:
  pull_request:
    paths-ignore:
      - '**/README.md'
    types: [opened, edited, synchronize, reopened]
  push:
    branches:
      - '*-maintenance'
      - 'release-notes-*'
    paths-ignore:
      - '**/README.md'

permissions:
  pull-requests: write

jobs:
  test-deploy:
    strategy:
      fail-fast: false
      matrix:
        playbook: ['playbook-product-local.yml', 'playbook-srfa-local.yml']
    uses: btat/product-docs-common/.github/workflows/test-deploy-min.yml@fa791c229bb230e966aaf4213fe961fc663b7ddb # main
    secrets:
      token: ${{ github.token }}
    with:
      playbook-name: ${{ matrix.playbook }}
